#include<stdio.h>
#include<stdlib.h>

struct Node {
    int data;
    struct Node* prev, * next;
};

void insert(struct Node* p, int x) {

}

int delete(struct Node* p) {
    
}

int main() {
    return 0;
}